# API Configuration
# Base URL for the Spring Boot backend service
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Mock API Toggle
# Set to 'true' to use the frontend mock layer, or 'false' to call the real backend
NEXT_PUBLIC_USE_MOCK_API=true
